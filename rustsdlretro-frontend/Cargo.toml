[package]
name = "rustsdlretro-frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
rustsdlretro-core = { path = "../rustsdlretro-core", default-features = false, features = ["fbdev"] }
libc = "0.2"
